# Agent Session — 2026-06-16 (Tooltip Feature)

## Goal
Add hover tooltips with 0.5s delay to all buttons across the Cardenveil UI, following a detailed spec for player-facing tooltips and deducing appropriate GM tooltips.

## Files Changed
| File | Changes |
|------|---------|
| `src/lib/tooltip.js` | **NEW** — Svelte action (83 lines): positioned tooltip div, 500ms delay, auto-flips at viewport edge, dark theme |
| `src/lib/PlayerHand.svelte` | Added `use:tooltip` to all buttons: draw, tokens, exchanges, race badges, Halfling/Tieffelin/Sporeling flows |
| `src/HandPopover.svelte` | Added `use:tooltip` to all buttons: draw, tokens, exchanges, suit picks, Tieffelin choice, Sporeling |
| `src/lib/GMDashboard.svelte` | Added `use:tooltip` to all 31 buttons: distribute, GM char, swap, export/import, hard reset |
| `src/lib/TokenPanel.svelte` | Added `use:tooltip` to spend/add buttons (8 total) |
| `src/lib/CardDisplay.svelte` | Replaced `title` with `use:tooltip` on action buttons |
| `src/lib/ActionLog.svelte` | Added `use:tooltip` to toggle button |

## Commands Run
- `npm run build` — passed (262 modules, no errors)
- `git checkout -b feat/tooltips`
- `git add -A && git commit` — 7 files, +205 / -7 lines
- `git checkout main && git merge feat/tooltips --no-ff`

## Key Design Decisions
- Svelte action pattern (`use:tooltip="text"`) chosen over wrapper component for minimal markup
- Tooltip: dark background (#1e1e2e), 11px text, max-width 220px, auto-flips if near top edge
- Dynamic tooltips for draw button (hand full vs pending Halfling vs normal)
- Token Carte/Combat tooltips match spec exactly (Force, Agilité, Esprit, Social)
- Race passive tooltips for all 5 races on the race badge
